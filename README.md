# Functions-Modules-in-Python

1.) Calculate Factorial Using a Function.py

Implements a recursive function to calculate the factorial of a number.Takes user input from the console.

2.)Using the Math Module for Calculations.py

Uses Python’s built-in math module to perform calculations.
Accepts a number from the user and calculates:
Square root,Logarithm (natural log),Sine value
